#!/usr/bin/env python3
from libdiscord.TP import DIS_TP
import sys

def main(FIT:str):
    t = DIS_TP(FIT)
    if (t.USER.HAS_GROUP("GODS")):
        t.TTS()
        t.TEXT("GOOD BYE")
        t.EXIT()
    else:
        t.TEXT("NEED ROOT")
    return

main(sys.argv[1])